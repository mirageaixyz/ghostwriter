{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["out/**", "dist/**"]
    },
    "edit": {
      "dependsOn": ["^produce"],
      "cache": false
    },
    "draft": {
      "dependsOn": ["^produce"],
      "cache": false
    }
  }
}